// Web stub — expo-image-picker is a native-only module.
// Metro resolves this file on web, and pickImage.ts on iOS/Android.

export async function pickImage(
  _aspect: [number, number],
): Promise<{ uri: string; mimeType?: string } | null> {
  return null;
}
